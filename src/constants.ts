// src/constants.ts

/**
 * Prefix for localStorage keys where click attempt data is stored.
 * The full key will be this prefix followed by a date key (e.g., YYYY-MM-DD).
 */
export const LOCAL_STORAGE_CLICK_ATTEMPT_PREFIX = 'click_attempt_';

// Add other widely used constants here as the application grows.
// For example:
// export const API_BASE_URL = '/api';
// export const DEFAULT_ANIMATION_DURATION = 0.3; // seconds
// export const MAX_USERNAME_LENGTH = 20;
